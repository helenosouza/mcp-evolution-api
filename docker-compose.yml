version: '3.8'

services:
  mcp_evolution:
    build:
      context: .
      dockerfile: Dockerfile
    image: mcp_evolution:latest
    # agora aponta diretamente para o .env recém-criado na build
    env_file:
      - .env
    ports:
      - "3000:3000"
    restart: unless-stopped
